namespace WCSharp.Events
{
	/// <summary>
	/// Defines all built-in <see cref="RegionUnitTypeEvent"/> supported by <see cref="PlayerUnitEvents"/>.
	/// <para>All of these events fire based on an unit type id filter.</para>
	/// </summary>
	public enum RegionUnitTypeEvent
	{
		Enters = 601,
		Leaves,
	}
}
